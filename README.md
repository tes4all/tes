# tes
TES Core. An open-source enterprise kernel in Go powering business apps through isolated plugins (TS/Bun). Versioned business flows with A/B testing, dry-run simulation, immutable event sourcing via NATS, and a schema registry built for AI/LLM integration. Never break a running system — by architecture.
